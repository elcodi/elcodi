knp_gaufrette:
    adapters:
        local:
            local:
                directory: %kernel.cache_dir%/local
                create: true
    filesystems:
        local:
            adapter: local
